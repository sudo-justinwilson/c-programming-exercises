#include <stdio.h>

#define CYCLE 32

int main()
{
	int letters[CYCLE * 2];
	int i= 0;

	for ( i = 'A'; i <= 'Z'; i++)
		printf(" %d", (((int)i) -0));
	putchar('\n');

	for ( i = 'a'; i <= 'z'; i++)
		printf(" %d", (((int)i) -0));

	for ( i = 0; i <= CYCLE; i++)
		letters

}
// 65 - 90, 97 - 122
